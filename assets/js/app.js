(()=>{const $=s=>document.querySelector(s),$$=s=>document.querySelectorAll(s);const html=document.documentElement;const saved=localStorage.getItem('theme');if(saved==='dark')html.classList.add('dark');const theme=$('#themeBtn');if(theme){theme.innerHTML=html.classList.contains('dark')?'<i class="fa-solid fa-sun"></i>':'<i class="fa-solid fa-moon"></i>';theme.addEventListener('click',()=>{html.classList.toggle('dark');localStorage.setItem('theme',html.classList.contains('dark')?'dark':'light');theme.innerHTML=html.classList.contains('dark')?'<i class="fa-solid fa-sun"></i>':'<i class="fa-solid fa-moon"></i>'})}const menu=$('#menuBtn'),mobile=$('#mobileNav');const setMenu=open=>{if(!menu||!mobile)return;mobile.classList.toggle('open',open);menu.setAttribute('aria-expanded',String(open));menu.setAttribute('aria-label',open?'إغلاق القائمة':'فتح القائمة');menu.innerHTML=open?'<i class="fa-solid fa-xmark"></i>':'<i class="fa-solid fa-bars"></i>'};if(menu){menu.setAttribute('aria-expanded','false');menu.addEventListener('click',e=>{e.stopPropagation();setMenu(!mobile.classList.contains('open'))})}document.addEventListener('click',e=>{if(mobile&&mobile.classList.contains('open')&&!mobile.contains(e.target)&&!menu.contains(e.target))setMenu(false)});document.addEventListener('keydown',e=>{if(e.key==='Escape')setMenu(false)});const progress=$('#progress');const scrollControls=document.createElement('div');scrollControls.className='scroll-controls';scrollControls.setAttribute('aria-label','أزرار التمرير');scrollControls.innerHTML='<button class="scroll-control" id="scrollTopBtn" type="button" aria-label="الانتقال إلى أعلى الصفحة" title="إلى الأعلى"><i class="fa-solid fa-chevron-up"></i></button><button class="scroll-control" id="scrollDownBtn" type="button" aria-label="الانتقال إلى أسفل الصفحة" title="إلى الأسفل"><i class="fa-solid fa-chevron-down"></i></button>';document.body.appendChild(scrollControls);const topBtn=$('#scrollTopBtn'),downBtn=$('#scrollDownBtn');const updateScrollUI=()=>{const max=document.documentElement.scrollHeight-innerHeight,y=scrollY;if(progress)progress.style.width=(max>0?y/max*100:0)+'%';scrollControls.classList.toggle('visible',y>220)};addEventListener('scroll',updateScrollUI,{passive:true});updateScrollUI();topBtn.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));downBtn.addEventListener('click',()=>scrollTo({top:document.documentElement.scrollHeight,behavior:'smooth'}));$$('.faq-q').forEach(q=>q.addEventListener('click',()=>{const item=q.parentElement;item.classList.toggle('open');const icon=q.querySelector('i');if(icon)icon.className=item.classList.contains('open')?'fa-solid fa-minus':'fa-solid fa-plus'}));const form=$('#contactForm');if(form)form.addEventListener('submit',e=>{e.preventDefault();const n=$('#name').value.trim(),s=$('#service').value,m=$('#message').value.trim();const text=`مرحباً Larache Web Dev،%0Aالاسم: ${encodeURIComponent(n)}%0Aالخدمة: ${encodeURIComponent(s)}%0Aالتفاصيل: ${encodeURIComponent(m)}`;open(`https://wa.me/212783539816?text=${text}`,'_blank','noopener')});const y=$('#year');if(y)y.textContent=new Date().getFullYear();const links=[...$$('.navlinks a,.mobile-nav a')];const activate=id=>links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')===`#${id}`));links.forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');const el=document.querySelector(id);if(el){e.preventDefault();setMenu(false);el.scrollIntoView({behavior:'smooth',block:'start'});history.replaceState(null,'',id);activate(id.slice(1))}}));const sections=[...$$('main section[id]')];const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)activate(entry.target.id)}),{rootMargin:'-25% 0px -65% 0px',threshold:0});sections.forEach(s=>observer.observe(s))})();